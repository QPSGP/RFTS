node_modules/
.next/
out/
dist/
.env*
data/*.json
!data/recording-descriptions.json
!data/blob-assets.json
# Local asset folders at repo root only (do not ignore public/covers for library placeholder SVG)
/Audios/
/Covers/
# Local schedule algorithm CSV/HTML exports (see npm run schedule:spreadsheet)
scripts/output/
.vercel
tmp/
*.tsbuildinfo
# Local SC client intake PDF (not shipped; process consent restricted)
docs/Client_Intake_Form_fillable.pdf
# Expo lead-card scan binaries stay local (extracts JSON is committed)
docs/lead-card-scans/**/*.jpg
docs/lead-card-scans/**/*.jpeg
docs/lead-card-scans/**/*.png
docs/lead-card-scans/**/*.PDF
docs/lead-card-scans/**/*.pdf
!docs/lead-card-scans/**/extracts.json
eng.traineddata
