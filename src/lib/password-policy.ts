/** New and changed passwords. Existing shorter passwords can still sign in. */
export const NEW_PASSWORD_MIN_LENGTH = 10;

export const NEW_PASSWORD_HINT = `Use at least ${NEW_PASSWORD_MIN_LENGTH} characters.`;
