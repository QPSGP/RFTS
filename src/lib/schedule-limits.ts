/** Max schedule nights the algorithm may generate / members may progress through. */
export const SCHEDULE_MAX_NIGHTS = 732;

/** Max main audios when counting 2 plays per night across the full horizon. */
export const SCHEDULE_MAX_MAIN_AUDIOS = SCHEDULE_MAX_NIGHTS * 2;
